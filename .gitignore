# Large data source files (not needed for the web app)
*.csv
*.tsv*
*.gz
Zoning.geojson
comps_r1r4.csv

# Build scripts (keep locally, not needed for web app)
*.py

# GeoJSON data (fire zones stamped into listings.js already)
fire_zones_vhfhsz.geojson

# TIGER shapefile cache (~120 MB)
tiger_urban/

# Local backup (pre-commit hook)
index.html.backup

# OS files
.DS_Store
.claude/
